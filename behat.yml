default:
  extensions:
    Behat\MinkExtension:
      base_url:  'http://youtube.com'
      default_session: selenium
      javascript_session: selenium
      selenium:
        host: localhost
        port: 4444